import { s } from '3oilerplate'

export const SApp = s.div(({ theme }: any) => ({
  fontFamily: 'base',
  color: 'white',
  backgroundColor: 'background',
  width: '100%',
  height: '100%',
  flexGrow: 1,
}))
